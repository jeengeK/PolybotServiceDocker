version: '3.9'

services:
  mongodb:
    image: mongo:4.4
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    networks:
      - app_network

  yolo5:
    build:
      context: ./yolo5  # Assuming Dockerfile is in the yolo5 directory
    container_name: yolo5
    restart: always
    ports:
      - "8081:8081"
    volumes:
      - ~/.aws:/root/.aws:ro  # Mount AWS credentials directory
    environment:
      AWS_REGION: us-east-1
      BUCKET_NAME: my-bucket-name  # Replace with your S3 bucket name
    networks:
      - app_network

volumes:
  mongodb_data:

networks:
  app_network:
